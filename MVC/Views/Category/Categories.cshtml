@model CategoryViewModel
@{
}

<div class="">
    <table class="table table-striped table-hover">
        <thead>
        </thead>
        <tbody>
            <tr>
                <td>@Model.Category.Name</td>
            </tr>
            @* @foreach (Category category in Model)
            {
                <tr>
                    <td class="list-group-item">@category.Name</td>
                </tr>
            } *@
        </tbody>
    </table>
</div>
